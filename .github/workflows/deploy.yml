name: TEMP - Show Secrets for Manual Setup

on:
  push:
    branches: [ main ]

jobs:
  show-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: 🚨 TEMPORARY - Display actual secret values
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          GOOGLE_AI_STUDIO_API_KEY: ${{ secrets.GOOGLE_AI_STUDIO_API_KEY }}
          CLOUDFLARE_AI_GATEWAY_TOKEN: ${{ secrets.CLOUDFLARE_AI_GATEWAY_TOKEN }}
          SECRETS_ENCRYPTION_KEY: ${{ secrets.SECRETS_ENCRYPTION_KEY }}
          WEBHOOK_SECRET: ${{ secrets.WEBHOOK_SECRET }}
        run: |
          echo "================================================"
          echo "🔍 ACTUAL SECRET VALUES (COPY THESE TO CLOUDFLARE):"
          echo "================================================"
          echo ""
          echo "CLOUDFLARE_API_TOKEN:"
          echo "$CLOUDFLARE_API_TOKEN"
          echo ""
          echo "CLOUDFLARE_ACCOUNT_ID:"
          echo "$CLOUDFLARE_ACCOUNT_ID"
          echo ""
          echo "GOOGLE_CLIENT_ID:"
          echo "$GOOGLE_CLIENT_ID"
          echo ""
          echo "GOOGLE_CLIENT_SECRET:"
          echo "$GOOGLE_CLIENT_SECRET"
          echo ""
          echo "JWT_SECRET:"
          echo "$JWT_SECRET"
          echo ""
          echo "GOOGLE_AI_STUDIO_API_KEY:"
          echo "$GOOGLE_AI_STUDIO_API_KEY"
          echo ""
          echo "CLOUDFLARE_AI_GATEWAY_TOKEN:"
          echo "$CLOUDFLARE_AI_GATEWAY_TOKEN"
          echo ""
          echo "SECRETS_ENCRYPTION_KEY:"
          echo "$SECRETS_ENCRYPTION_KEY"
          echo ""
          echo "WEBHOOK_SECRET:"
          echo "$WEBHOOK_SECRET"
          echo ""
          echo "================================================"
          echo "📋 Copy the values above to Cloudflare Dashboard"
          echo "📋 Then we'll restore the secure deployment workflow"
          echo "================================================"
          
      - name: Generate missing secrets
        run: |
          echo ""
          echo "🔧 GENERATED VALUES FOR MISSING SECRETS:"
          echo "========================================"
          echo ""
          echo "SECRETS_ENCRYPTION_KEY (32 chars):"
          openssl rand -hex 32
          echo ""
          echo "WEBHOOK_SECRET:"
          openssl rand -hex 16
          echo ""
          echo "JWT_SECRET (if yours is empty):"
          openssl rand -hex 32
          echo ""
          echo "========================================"
          echo "✅ Use these if any secrets above were empty"
