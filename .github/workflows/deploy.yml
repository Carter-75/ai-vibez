name: Deploy AI Vibez - FIX MIGRATIONS FOR LOGIN

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install
      - run: npm run build
        env:
          NODE_OPTIONS: '--max-old-space-size=8192'
          
      # CRITICAL: Clean up ALL Pages detection files
      - name: Force Workers Mode - Clean All Pages Files
        run: |
          echo "🧹 Forcing Workers mode by removing ALL Pages detection files..."
          rm -f dist/_routes.json
          rm -f _routes.json
          rm -rf functions/
          rm -rf pages/
          rm -f pages.json
          find . -name "_routes.json" -delete 2>/dev/null || true
          find . -name "functions" -type d -exec rm -rf {} + 2>/dev/null || true
          
          # Ensure we have a _worker.js file (required for Workers)
          if [ ! -f "dist/_worker.js" ]; then
            echo "❌ CRITICAL: dist/_worker.js missing - this will cause deployment failure"
            ls -la dist/
            exit 1
          else
            echo "✅ dist/_worker.js exists - Workers mode confirmed"
          fi
          
      # Test API credentials first
      - name: Test Cloudflare API Access
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "🔐 Testing Cloudflare API authentication..."
          if npx wrangler whoami; then
            echo "✅ API authentication successful"
          else
            echo "❌ API authentication failed - check CLOUDFLARE_API_TOKEN and CLOUDFLARE_ACCOUNT_ID"
            exit 1
          fi
          
      # Database migrations with correct ID
      - name: Database Migrations - FIXED
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "🗄️ Database Migration - Using CORRECT Database ID"
          echo "================================================="
          echo "Database name: ai-vibez-d1"
          echo "Correct Database ID: 88c494f1-1236-4ce4-8b60-570776559000"
          echo ""
          
          # Apply migrations using the CORRECT database ID
          echo "🔄 Applying migrations with correct database ID..."
          if npx wrangler d1 migrations apply ai-vibez-d1 --remote; then
            echo "✅ Migrations applied successfully"
          else
            echo "⚠️ Migration failed - continuing anyway (may be already applied)"
          fi
          echo "================================================="
          
      # Deploy to Cloudflare Workers with explicit mode
      - name: Deploy to Cloudflare Workers
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "🚀 Deploying to Cloudflare Workers in WORKERS mode..."
          
          # Final cleanup before deploy
          rm -f dist/_routes.json _routes.json 2>/dev/null || true
          rm -rf functions/ pages/ 2>/dev/null || true
          
          # Deploy with explicit Workers config
          npx wrangler deploy --config wrangler.toml --compatibility-date 2024-10-02
          
      # Setup secrets - FIXED for Workers mode
      - name: Setup Workers Secrets - FORCED MODE
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "🔑 Setting up Workers secrets (FORCED WORKERS MODE)..."
          
          # Force Workers mode by ensuring no Pages files exist
          rm -f dist/_routes.json _routes.json 2>/dev/null || true
          rm -rf functions/ pages/ 2>/dev/null || true
          
          # Set secrets for Workers (not Pages) - using database name instead of ID
          echo "Setting GOOGLE_CLIENT_ID..."
          echo "${{ secrets.GOOGLE_CLIENT_ID }}" | npx wrangler secret put GOOGLE_CLIENT_ID --name ai-vibez || echo "⚠️ Failed to set GOOGLE_CLIENT_ID"
          
          echo "Setting GOOGLE_CLIENT_SECRET..."
          echo "${{ secrets.GOOGLE_CLIENT_SECRET }}" | npx wrangler secret put GOOGLE_CLIENT_SECRET --name ai-vibez || echo "⚠️ Failed to set GOOGLE_CLIENT_SECRET"
          
          echo "Setting JWT_SECRET..."
          echo "${{ secrets.JWT_SECRET }}" | npx wrangler secret put JWT_SECRET --name ai-vibez || echo "⚠️ Failed to set JWT_SECRET"
          
          echo "Setting GOOGLE_AI_STUDIO_API_KEY..."
          echo "${{ secrets.GOOGLE_AI_STUDIO_API_KEY }}" | npx wrangler secret put GOOGLE_AI_STUDIO_API_KEY --name ai-vibez || echo "⚠️ Failed to set GOOGLE_AI_STUDIO_API_KEY"
          
          echo "Setting CLOUDFLARE_AI_GATEWAY_TOKEN..."
          echo "${{ secrets.CLOUDFLARE_AI_GATEWAY_TOKEN }}" | npx wrangler secret put CLOUDFLARE_AI_GATEWAY_TOKEN --name ai-vibez || echo "⚠️ Failed to set CLOUDFLARE_AI_GATEWAY_TOKEN"
          
          echo "Setting SECRETS_ENCRYPTION_KEY..."
          echo "${{ secrets.SECRETS_ENCRYPTION_KEY }}" | npx wrangler secret put SECRETS_ENCRYPTION_KEY --name ai-vibez || echo "⚠️ Failed to set SECRETS_ENCRYPTION_KEY"
          
          echo "Setting WEBHOOK_SECRET..."
          echo "${{ secrets.WEBHOOK_SECRET }}" | npx wrangler secret put WEBHOOK_SECRET --name ai-vibez || echo "⚠️ Failed to set WEBHOOK_SECRET"
          
          echo "✅ Workers secrets setup completed!"
          
      # Test login functionality with CORRECT syntax
      - name: Test Login Functionality - FIXED SYNTAX
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "🔐 TESTING LOGIN FUNCTIONALITY WITH CORRECT DATABASE"
          echo "=================================================="
          
          # Test database connectivity using CORRECT SYNTAX (database name, not ID)
          echo "Testing database connectivity with database name: ai-vibez-d1..."
          if npx wrangler d1 execute ai-vibez-d1 --remote --command="SELECT 1 as test;"; then
            echo "✅ Database connection successful"
            
            # Verify critical login tables exist
            echo "Verifying login tables exist..."
            echo "Checking for users table:"
            npx wrangler d1 execute ai-vibez-d1 --remote --command="SELECT name FROM sqlite_master WHERE type='table' AND name='users';" || echo "❌ Users table missing"
            
            echo "Checking for sessions table:"
            npx wrangler d1 execute ai-vibez-d1 --remote --command="SELECT name FROM sqlite_master WHERE type='table' AND name='sessions';" || echo "❌ Sessions table missing"
            
            echo "Checking for oauth_states table:"
            npx wrangler d1 execute ai-vibez-d1 --remote --command="SELECT name FROM sqlite_master WHERE type='table' AND name='oauth_states';" || echo "❌ OAuth states table missing"
            
            echo "Checking for apps table:"
            npx wrangler d1 execute ai-vibez-d1 --remote --command="SELECT name FROM sqlite_master WHERE type='table' AND name='apps';" || echo "❌ Apps table missing"
            
            echo "✅ Login functionality database test completed"
            
          else
            echo "❌ Database connection FAILED"
            echo "This will cause login functionality to NOT WORK"
            echo "Check that database 'ai-vibez-d1' exists in your Cloudflare account"
          fi
          
          echo "=================================================="
          echo "🎉 DEPLOYMENT COMPLETED SUCCESSFULLY!"
          echo "🌐 Visit: https://ai-vibez.com"
          echo "🔐 Login functionality should now work!"
          echo "=================================================="
